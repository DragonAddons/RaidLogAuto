name: Luacheck

on:
  pull_request:
  push:
    branches: [master]

jobs:
  luacheck:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run luacheck
        uses: lunarmodules/luacheck@v1.2.0
